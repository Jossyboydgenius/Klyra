export const metadata = {
  title: 'Cross-Chain Payment | Klyra',
  description: 'Send any token, recipient receives any token on any chain',
};

export default function PaymentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

