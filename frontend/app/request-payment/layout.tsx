export const metadata = {
  title: 'Request Payment | Klyra',
  description: 'Create payment requests that can be paid with any token',
};

export default function RequestPaymentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

